interface:
  display_name: "АРТЕЛЬ"
  short_description: "Standalone Revit agent and family factory"
  default_prompt: "Use $agnostis to work on standalone ARTEL. Treat LES as an optional evidence integration, not a runtime dependency."
